-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.clientes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL,
  status text DEFAULT 'ativo'::text,
  created_at timestamp with time zone DEFAULT now(),
  empresa_id uuid,
  perfil_id uuid,
  CONSTRAINT clientes_pkey PRIMARY KEY (id),
  CONSTRAINT clientes_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.empresas(id),
  CONSTRAINT clientes_perfil_id_fkey FOREIGN KEY (perfil_id) REFERENCES public.perfis(id)
);
CREATE TABLE public.empresas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  razao_social text NOT NULL,
  nome_fantasia text,
  cnpj text NOT NULL UNIQUE,
  status text DEFAULT 'ativo'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  email text,
  telefone text,
  CONSTRAINT empresas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.logs_auditoria (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  nome_usuario text,
  tipo_acao text NOT NULL CHECK (tipo_acao = ANY (ARRAY['INSERT'::text, 'UPDATE'::text, 'DELETE'::text])),
  tabela_afetada text NOT NULL,
  object_id text NOT NULL,
  dados_antigos jsonb DEFAULT '{}'::jsonb,
  dados_novos jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT logs_auditoria_pkey PRIMARY KEY (id),
  CONSTRAINT logs_auditoria_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.motoristas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL,
  cpf text UNIQUE,
  telefone text,
  cnh text,
  validade_cnh date,
  chave_pix text,
  tipo_vinculo text DEFAULT 'terceiro'::text CHECK (tipo_vinculo = ANY (ARRAY['fixo'::text, 'terceiro'::text])),
  status text DEFAULT 'ativo'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  pix_key text,
  categoria_cnh text,
  email text,
  status_operacional text DEFAULT 'disponível'::text CHECK (status_operacional = ANY (ARRAY['disponível'::text, 'em_viagem'::text, 'férias'::text, 'afastado'::text])),
  perfil_id uuid,
  CONSTRAINT motoristas_pkey PRIMARY KEY (id),
  CONSTRAINT motoristas_perfil_id_fkey FOREIGN KEY (perfil_id) REFERENCES public.perfis(id)
);
CREATE TABLE public.notificacoes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  titulo text NOT NULL,
  mensagem text NOT NULL,
  tipo text DEFAULT 'info'::text CHECK (tipo = ANY (ARRAY['info'::text, 'success'::text, 'warning'::text, 'error'::text])),
  link text,
  lida boolean DEFAULT false,
  metadata jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT notificacoes_pkey PRIMARY KEY (id),
  CONSTRAINT notificacoes_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.ordem_servico_paradas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ordem_id uuid NOT NULL,
  endereco_ponto text NOT NULL,
  horario_previsto timestamp without time zone,
  observacoes text,
  ordem_parada integer NOT NULL,
  realizada boolean DEFAULT false,
  horario_realizado timestamp without time zone,
  latitude numeric,
  longitude numeric,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT ordem_servico_paradas_pkey PRIMARY KEY (id),
  CONSTRAINT ordem_servico_paradas_ordem_id_fkey FOREIGN KEY (ordem_id) REFERENCES public.ordens_servico(id)
);
CREATE TABLE public.ordens_servico (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  numero_os text UNIQUE,
  empresa_id uuid NOT NULL,
  cliente_final_id uuid,
  motorista_id uuid,
  veiculo_id uuid,
  nome_passageiro text,
  origem text,
  destino text,
  data_execucao timestamp without time zone NOT NULL,
  horario_inicio timestamp without time zone,
  horario_fim timestamp without time zone,
  valor_faturamento numeric DEFAULT 0,
  valor_custo_motorista numeric DEFAULT 0,
  status text DEFAULT 'pendente'::text CHECK (status = ANY (ARRAY['pendente'::text, 'em_andamento'::text, 'concluido'::text, 'cancelado'::text])),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  tarifario_id uuid,
  passageiro text,
  voucher text,
  origem_latitude numeric,
  origem_longitude numeric,
  destino_latitude numeric,
  destino_longitude numeric,
  trajeto_manual boolean DEFAULT false,
  tipo_cobranca text,
  possui_retorno boolean DEFAULT false,
  classificacao_trajeto text,
  resumo_financeiro text,
  tempo_hora_parada interval DEFAULT '00:00:00'::interval,
  valor_unitario_hora_parada numeric DEFAULT 0,
  valor_total_hora_parada numeric DEFAULT 0,
  obs_hora_parada text,
  atividade_motivo text,
  divisao_departamento text,
  autorizado_por text,
  km_inicial numeric,
  km_final numeric,
  km_total_rodado numeric,
  valor_custo_estacionamento numeric DEFAULT 0,
  valor_custo_pedagio numeric DEFAULT 0,
  valor_custo_extra_terceiros numeric DEFAULT 0,
  numero_nfe text,
  centro_custo_cliente text,
  numero_requisicao_bu text,
  forma_faturamento text,
  numero_cte text,
  observacoes_financeiras text,
  conferida_financeiro boolean DEFAULT false,
  observacoes_gerais text,
  CONSTRAINT ordens_servico_pkey PRIMARY KEY (id),
  CONSTRAINT ordens_servico_tarifario_id_fkey FOREIGN KEY (tarifario_id) REFERENCES public.tarifarios(id),
  CONSTRAINT ordens_servico_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.empresas(id),
  CONSTRAINT ordens_servico_cliente_final_id_fkey FOREIGN KEY (cliente_final_id) REFERENCES public.clientes(id),
  CONSTRAINT ordens_servico_motorista_id_fkey FOREIGN KEY (motorista_id) REFERENCES public.motoristas(id),
  CONSTRAINT ordens_servico_veiculo_id_fkey FOREIGN KEY (veiculo_id) REFERENCES public.veiculos(id)
);
CREATE TABLE public.perfis (
  id uuid NOT NULL,
  email text,
  full_name text,
  avatar_url text,
  role text DEFAULT 'operator'::text CHECK (role = ANY (ARRAY['admin'::text, 'operador'::text, 'motorista'::text, 'cliente'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  nome text,
  aprovado_operador boolean DEFAULT false,
  aprovado_por uuid,
  CONSTRAINT perfis_pkey PRIMARY KEY (id),
  CONSTRAINT perfis_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.recebimentos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  ordem_id uuid NOT NULL,
  valor numeric NOT NULL,
  data_pagamento date,
  forma_pagamento text,
  status text DEFAULT 'pendente'::text CHECK (status = ANY (ARRAY['pendente'::text, 'pago'::text, 'atrasado'::text])),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT recebimentos_pkey PRIMARY KEY (id),
  CONSTRAINT recebimentos_ordem_id_fkey FOREIGN KEY (ordem_id) REFERENCES public.ordens_servico(id)
);
CREATE TABLE public.tarifarios (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  origem text NOT NULL,
  destino text NOT NULL,
  valor_venda numeric NOT NULL,
  valor_custo numeric,
  descricao text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  km_previsto numeric,
  custo_previsto_combustivel numeric DEFAULT 0,
  custo_previsto_pedagio numeric DEFAULT 0,
  custo_previsto_motorista numeric DEFAULT 0,
  CONSTRAINT tarifarios_pkey PRIMARY KEY (id)
);
CREATE TABLE public.veiculo_abastecimentos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  veiculo_id uuid NOT NULL,
  ordem_id uuid,
  data_abastecimento timestamp with time zone NOT NULL,
  km_abastecimento numeric NOT NULL,
  litros numeric NOT NULL,
  valor_total numeric NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT veiculo_abastecimentos_pkey PRIMARY KEY (id),
  CONSTRAINT veiculo_abastecimentos_veiculo_id_fkey FOREIGN KEY (veiculo_id) REFERENCES public.veiculos(id),
  CONSTRAINT veiculo_abastecimentos_ordem_id_fkey FOREIGN KEY (ordem_id) REFERENCES public.ordens_servico(id)
);
CREATE TABLE public.veiculo_manutencoes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  veiculo_id uuid NOT NULL,
  tipo_manutencao text NOT NULL CHECK (tipo_manutencao = ANY (ARRAY['preventiva'::text, 'corretiva'::text])),
  descricao text NOT NULL,
  data_manutencao date NOT NULL,
  km_manutencao numeric,
  valor_total numeric NOT NULL,
  realizada_por text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT veiculo_manutencoes_pkey PRIMARY KEY (id),
  CONSTRAINT veiculo_manutencoes_veiculo_id_fkey FOREIGN KEY (veiculo_id) REFERENCES public.veiculos(id)
);
CREATE TABLE public.veiculos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  placa text NOT NULL UNIQUE,
  modelo text,
  ano integer,
  meta_faturamento_mensal numeric DEFAULT 0,
  status text DEFAULT 'ativo'::text,
  created_at timestamp with time zone DEFAULT now(),
  meta_faturamento numeric DEFAULT 0,
  capacidade bigint,
  km_atual numeric,
  km_proxima_revisao numeric,
  data_proxima_revisao date,
  km_litro_medio_real numeric,
  status_operacional text DEFAULT 'disponível'::text CHECK (status_operacional = ANY (ARRAY['disponível'::text, 'em_viagem'::text, 'manutenção'::text, 'quebrado'::text])),
  ultima_latitude numeric,
  ultima_longitude numeric,
  imagem_veiculo text,
  CONSTRAINT veiculos_pkey PRIMARY KEY (id)
);